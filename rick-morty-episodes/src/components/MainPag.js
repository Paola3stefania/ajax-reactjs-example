import React, { Component } from "react";

export default class MainPag extends Component {
	render() {
		return (
			<nav>
				{" "}
				Im the main pagination
				{/* //TODO tendria q tener un next con la paginacion de next de la info. el
						link con el next pasarle el relPATH a Data pero que se quede dentro
						del mismo container */}
			</nav>
		);
	}
}
